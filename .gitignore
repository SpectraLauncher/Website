
CLAUDE.md

# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Local databases (telemetry, share packs, accounts) — never in git
data

# Test archives. The small ones are written by test/fixtures/make.py, the real
# mods and schematics are other people's builds — neither belongs in git.
test/fixtures/*.jar
test/fixtures/*.zip
test/fixtures/*.mrpack
test/fixtures/*.nbt
test/fixtures/*.litematic
test/fixtures/*.schem
test/fixtures/*.schematic
